/**
 * Core service to create, edit, export the local identity.
 */
export class LocalIdentityManager {
    public static showManagementScreen() {

    }

    /**
     * Starts the local identity creation process, including local DID creation, DID publishing,
     * hive setup, etc. This process takes several minutes.
     */
    public static startIdentityCreationProcess() {

    }
}